import AdminApp from "../../components/admin/AdminApp";

export const metadata = { title: "Camera fleet · Plant Health AI", robots: { index: false, follow: false } };

export default function AdminPage() {
  return <AdminApp />;
}
